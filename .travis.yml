---
language: node_js
sudo: false

nvm:
  - "5.9.1"

cache:
  directories:
    - node_modules
    - bower_components

install:
  - "npm install"
  - "bower install"

script:
  - gulp spec

notifications:
  email: false
